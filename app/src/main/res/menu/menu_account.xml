<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
        <item
            android:id="@+id/menu_item_add_transaction_scheduler"
            android:icon="@android:drawable/ic_menu_add"
            android:title="@string/add_transaction_scheduler"
            app:showAsAction="ifRoom" />
        <item
            android:id="@+id/menu_item_edit_account"
            android:icon="@android:drawable/ic_menu_edit"
            android:title="Edit Account"
            app:showAsAction="never|withText"
            />
        <item
            android:id="@+id/menu_item_remove_account"
            android:icon="@android:drawable/ic_menu_delete"
            android:title="Delete Account"
            app:showAsAction="never|withText"
            />
        <item
            android:id="@+id/menu_item_update_forecast"
            android:title="@string/update_forecast_date"
            app:showAsAction="never|withText" />
</menu>